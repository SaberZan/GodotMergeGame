/**
 * created by SaberZan
 * 帧同步ECS系统
 */

import { EntitySystem } from "./EntitySystem";

export class ExecuteSystem extends EntitySystem {
    /**
     * 这个系统被促发
     * @param world 
     * @param cmd 
    */
    public Tick(cmd: any) {

    }

}
